apiVersion: apps/v1
kind: Deployment
metadata:
  name: ml-platform-admin-web
  namespace: ml-platform-system
  labels:
    app: ml-platform-admin-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ml-platform-admin-web
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: ml-platform-admin-web
    spec:
      automountServiceAccountToken: false
      containers:
        - command:
            - ml-platform-admin-web
            - --static-dir=/static
            - --insecure-bind-address=0.0.0.0
            - --bind-address=0.0.0.0
            - --dashboard-config-path=/config/dashboard-config.yaml
          name: ml-platform-admin-web
          image: docker.io/lehuannhatrang/ml-platform-admin-web:v0.15
          imagePullPolicy: IfNotPresent
          env:
            - name: GIN_MODE
              value: release
          livenessProbe:
            failureThreshold: 8
            httpGet:
              path: /livez
              port: 8000
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 30
            successThreshold: 1
            timeoutSeconds: 15
          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /readyz
              port: 8000
              scheme: HTTP
            periodSeconds: 30
            successThreshold: 1
            timeoutSeconds: 15
          resources:
            requests:
              cpu: 250m
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          volumeMounts:
            - name: kubeconfig
              subPath: kubeconfig
              mountPath: /etc/kubeconfig
            - name: dashboard-config
              subPath: prod.yaml
              mountPath: /config/dashboard-config.yaml
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      volumes:
        - name: kubeconfig
          secret:
            secretName: kubeconfig
        - name: dashboard-config
          configMap:
            name:  ml-platform-admin-configmap

---
apiVersion: v1
kind: Service
metadata:
  name: ml-platform-admin-web
  namespace: ml-platform-system
  labels:
    app: ml-platform-admin-web
spec:
  ports:
    - name: ml-platform-admin-web
      port: 8000
      protocol: TCP
      targetPort: 8000
      nodePort: 32000
  selector:
    app: ml-platform-admin-web
  type: NodePort