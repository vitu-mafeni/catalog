apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: knative-config
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: Site-specific Knative configuration. Edit config-domain.yaml with the workload cluster master IP before approving.
pipeline:
  mutators:
    - image: gcr.io/kpt-fn/starlark:v0.5.0
      configPath: add-depends-on.yaml
