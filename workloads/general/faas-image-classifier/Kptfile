apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: image-classifier
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: Image classifier FaaS application using Knative serving with scale-to-zero
status:
  conditions:
    - type: Rendered
      status: "True"
      reason: RenderSuccess
